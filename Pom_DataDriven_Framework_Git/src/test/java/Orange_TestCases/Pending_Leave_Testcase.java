package Orange_TestCases;

import CommonFunctions.Base;

public class Pending_Leave_Testcase extends Base{
	/*
	
//	String actual_message=null;

	public void login()
	{
		
		PageFactory.initElements(driver, Login_Pom.class);
		Login_Pom.username.sendKeys(properties.getProperty("username"));
		Login_Pom.Password.sendKeys(properties.getProperty("password"));
		Login_Pom.LoginButton.click();
	}
	
	public void getpendingleave_request()
	{
		PageFactory.initElements(driver, Dashboard_PageObjects.class);
		String actual_message=Dashboard_PageObjects.pending_leave_request.getText();
	}
	
	@Test
	public void verifypendingleaverequest()
	{
		login();
	//	getpendingleave_request();
		PageFactory.initElements(driver, Dashboard_PageObjects.class);
		String actual_message=Dashboard_PageObjects.pending_leave_request.getText();
		System.out.println("the msg is"+actual_message);
		Assert.assertEquals(actual_message, "No Records are Available");
		
		
	}
	*/
}